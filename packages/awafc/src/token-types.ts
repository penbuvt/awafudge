export enum TokenType {
  RightShift = 'RIGHT_SHIFT',
  LeftShift = 'LEFT_SHIFT',
  Increment = 'INCREMENT',
  Decrement = 'DECREMENT',
  Write = 'WRITE',
  Read = 'READ',
  Loop = 'LOOP',
}

export default TokenType;
